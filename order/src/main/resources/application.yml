spring:
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    publisher-confirms: true #  消息发送到交换机确认机制,是否确认回调
    publisher-returns: true
    virtual-host: yzd #  virtual-host:/ 默认值；消息发送到交换机确认机制,是否返回回馈
  devtools:
    restart:
      enabled: false # 热部署
server:
  port: 90
